{
  "scripts": {
    "test": "run-s test:**",
    "test:eslint": "eslint --fix --no-error-on-unmatched-pattern  .",
    "test:stylelint": "stylelint --fix --allow-empty-input \"**/*.css\""
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.2.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0",
    "npm-run-all": "^4.1.5",
    "stylelint": "^13.6.1",
    "stylelint-config-standard": "^20.0.0"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "ffmpeg": "0.0.4",
    "ffmpeg-extract-frames": "^2.0.2",
    "ffmpeg-fork": "0.0.6"
  }
}
